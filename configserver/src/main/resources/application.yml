server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/mzxlgx/springCloud.git
          searchPaths: config
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

