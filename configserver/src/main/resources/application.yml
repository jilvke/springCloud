server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jilvke/springCloud.git
          searchPaths: config
  application:
    name: config-Server
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

